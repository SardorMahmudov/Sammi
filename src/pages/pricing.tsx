import { withLayout } from "../layout/layout";
import { PrisingPageComponent } from "../page-component";

const Pricing = () => {
  return <PrisingPageComponent />;
};

export default withLayout(Pricing);
